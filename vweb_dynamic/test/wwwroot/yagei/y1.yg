package main

import(
	"this"
	"fmt"
	"time"
	pkg "github.com/foo/pkg"
)

var i int
func Main(a interface{}) string {
	pkg.NewSample()()
	fmt.Println(this.IsNil(nil))
	
	if i == 0 {
		i++
		time.Sleep(time.Second)
	}
	fmt.Printf("%#v %v\n",a, a)
	
	
	return "789\n"
}

